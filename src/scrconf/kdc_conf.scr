/**
 * File:
 *   kdc_conf.scr
 * Summary:
 *   SCR Agent for reading/writing kdc.conf using the ini-agent
 *
 * $Id: $
 *
 * Read/Sets the values defined in <tt>kdc.conf</tt>.
 */
.kdc_conf
`ag_kerberos (
  `INIT ($[
    "file"      : "/var/lib/kerberos/krb5kdc/kdc.conf"
  ])
)
